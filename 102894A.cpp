#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n, x, y, t, mi = 0, ma = 0; cin >> n >> x >> y;
    while(n--) {
        cin >> t;
        if (t > x) && (t - x > y)
    }
    return 0;
}